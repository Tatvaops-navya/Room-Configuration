/**
 * Experimental / disabled product paths. Kept for future re-enable; do not delete.
 * @see ConfigurationSelector — component-based arrangement UI when true.
 */
export const COMPONENT_BASED_CONFIG_ENABLED = false
