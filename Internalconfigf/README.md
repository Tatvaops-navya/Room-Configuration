
  # internal config.f

  This is a code bundle for internal config.f. The original project is available at https://www.figma.com/design/4xn2NEBTylm4ccMRTjlOWi/internal-config.f.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  